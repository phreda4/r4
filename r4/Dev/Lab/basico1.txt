| test para compilador
#v1 1

#ccx #ccy
#rom8x12 0 0 0
:scanline
	;

:char8id | c --
	ccx ccy setxy
	3 << dup 2/ + 'rom8x12 +
	sw 16 - >r
	12 ( 1? )( 1- swap c@+  |cnt adr bit
		$80 ( 1? )( dup >r scanline r> 2/ ) drop
		r px+!
		$80 ( 1? )( dup >r scanline r> 2/ ) drop
		drop
		r px+!
		swap )
	2drop rdrop
	;


:main
	(
	0 dup setxy
	sw sh * ( 1? )( v1 px!+ 1- ) drop
	redraw
	update
	key 1 =? ( drop ; ) drop
	$10101 'v1 +!
char8id
	)
	;

: main ;